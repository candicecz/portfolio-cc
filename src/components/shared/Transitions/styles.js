import styled from "styled-components";
import { animated } from "react-spring";

export const StyledAnimated = styled(animated.div)`
  width: 100%;
  height: 100%;
  position: absolute;
  top: 0;
  left: 0;
`;
